# denoise - 2026-04-18

## TODO

1. ~~**Evaluate dndetmap once Apollo training completes.**~~ Done. iter_43900: NDS=0.543, mAP=0.425 — matches rot3dv2 baseline, no regression vs aux2d.
2. ~~**Re-run dndetmap with coordinate bug fixed — map plateau under investigation.**~~ Done. The control runs closed the question: the plateau is **not DN-specific**. `stage1_4gpu_bs32` without DN still plateaus at 8/GPU, while `stage1_4gpu_bs24` without DN converges at 6/GPU. The dndetmap coordinate fix restored meaningful DN gradients, but the 4-GPU 8/GPU stage-1 setup remains unstable for map learning.
3. **If prediction DN helps at stage2: try applying it at multiple decoder stages.** Currently only supervised at the final refine layer — marginal gains there may compound if the DN signal reaches earlier representations.
4. ~~If planning DN helps, combine with prediction DN.~~ Planning DN hurts — do not pursue.
5. **Consider joint det + map + prediction DN** once items 2–3 are resolved.

## Intro

Denoising has been shown to improve detection training by injecting GT-initialized noisy anchors as extra supervised queries. This experiment extends the denoising framework to the map, prediction, and planning heads to test whether the same benefit generalizes.

## Method

**Map DN** (`SparsePoint3DTarget.get_dn_anchors`):
- Implemented `get_dn_anchors()` in `SparsePoint3DTarget`, mirroring the detection DN structure.
- GT map polylines are normalized to [0,1] BEV space, then perturbed with noise (`dn_noise_scale=0.1`, ≈3m in BEV).
- 3 positive DN groups + negative DN (larger noise) are created per frame.
- DN anchors flow through the full Sparse4DHead decoder and are supervised with the existing DN loss infrastructure.
- Config: `sparsedrive_r50_stage1_8gpu_noflash_dndetmap` (base: dn config which already has det DN at `num_dn_groups=5`).

**Prediction DN** (`MotionPlanningHead._build_dn_pred_queries`):
- GT agent future trajectories are perturbed with Gaussian noise (`dn_pred_noise_scale=0.5`, `num_dn_pred_groups=3`).
- Each DN group produces one token per GT agent: zero-initialized instance feature, zero anchor embed, and a mode query encoded from the noisy trajectory's cumulative endpoint via `motion_anchor_encoder`.
- Token layout in the decoder: `[agents | ego | DN_agents | DN_egos]`. DN tokens flow through the full GNN/temporal/cross-attention decoder as queries (normal tokens are always keys — no DN contamination of normal attention).
- At the final refine layer, DN agent features are combined with their 1-mode queries and passed through `motion_reg_branch` to produce trajectory predictions. These are supervised with cumulative L1 loss against GT (`dn_pred_loss_weight=1.0`).
- Config: `sparsedrive_r50_stage2_4gpu_bs24_dnpred` (base: vanilla stage2 baseline).

**Planning DN** (`MotionPlanningHead._build_dn_plan_queries`):
- GT ego future trajectory is perturbed with noise (`dn_plan_noise_scale=0.5`, `num_dn_plan_groups=3`).
- Each DN group produces one ego token: zero-initialized feature, ego anchor embed tiled from the real ego, and a 1-mode query from the noisy trajectory's cumulative endpoint via `plan_anchor_encoder`.
- DN ego tokens flow through the full decoder alongside the real ego token. The real ego token slice (`[num_anchor:num_anchor+1]`) is used exclusively for the normal plan head; DN ego tokens are separated out after the loop.
- Supervised with cumulative L1 loss (`dn_plan_loss_weight=1.0`).
- Config: `sparsedrive_r50_stage2_4gpu_bs24_dnplan` (base: vanilla stage2 baseline).

**Key design choice**: DN tokens flow through the full GNN/temporal/cross-attention decoder, giving them contextual features before regression. No attention mask is needed because GNN keys are always the normal top-k tokens — DN tokens never appear as keys. The refine step explicitly slices only the real ego token so DN tokens do not corrupt the normal plan head. Only 1 trajectory mode is used per DN token (vs. `fut_mode=6` for normal agents), initialized from the noisy GT endpoint.

## Results

| Config | Server | Job ID | NDS | mAP | map mAP | car EPA | L2 (avg) | obj_box_col (avg) | Notes |
|--------|--------|--------|-----|-----|---------|---------|----------|-------------------|-------|
| **stage1** | | | | | | | | | |
| stage1_8gpu_noflash | apollo | — | 0.5307 | 0.4137 | 0.577 | — | — | — | stage1 baseline |
| stage1_8gpu_noflash_dn | apollo | — | 0.5368 | 0.4227 | 0.559 | — | — | — | det DN only |
| stage1_8gpu_noflash_dndetmap | apollo | — | 0.5433 | 0.4249 | 0.014 | — | — | — | det DN + map DN; map mAP collapse due to double-normalization bug (fixed) |
| stage1_4gpu_bs32_dndetmap | narval | 59688618 (smoke) / 59688619 | — | — | — | — | — | — | fixed DN; DN losses healthy (`map_loss_line_dn_5 ≈ 0.12`), but map still plateaus at 8/GPU |
| stage1_4gpu_bs32 | narval | 59696538 | — | — | — | — | — | — | no-DN 8/GPU control; same map plateau, so failure is not DN-specific |
| stage1_4gpu_bs24 | narval | 59696692 | — | — | — | — | — | — | no-DN 6/GPU control; converges as expected, consistent with April 1 findings |
| **stage2** | | | | | | | | | |
| stage2_4gpu_bs24 | — | — | 0.5232 | 0.4132 | 0.553 | 0.492 | 0.636 | 0.133% | stage2 baseline |
| stage2_4gpu_bs24_dnpred | narval | 59564082 | 0.5304 | 0.4177 | — | — | — | — | training crashed (exitcode 1 after ~5h); no eval run |
| stage2_4gpu_bs24_dnpred | dgx | 3601 (train) / 3606 (eval) | 0.5290 | 0.4170 | — | 0.496 | 0.628 | 0.135% | |
| stage2_4gpu_bs24_dnplan | narval | 59605629 | 0.5261 | 0.4134 | 0.549 | — | 0.670 | 0.111% | |
| stage2_4gpu_bs24_dnplan | dgx | 3607 | 0.5190 | 0.4042 | 0.553 | 0.488 | 0.674 | 0.123% | |

## Discussion

**Prediction DN (DGX job 3606) — complete.** dnpred produces marginal gains over the baseline: NDS 0.529 vs 0.523 (+0.006), mAP 0.417 vs 0.413 (+0.004), car_epa 0.496 vs 0.492 (+0.004). Planning L2 is 0.628 vs 0.636 (−0.008) and collision is essentially flat (0.135% vs 0.133%). The detection and motion improvements are small but consistent, suggesting prediction DN provides a mild regularisation benefit. However, the planning gain is negligible — prediction DN alone is not sufficient to drive L2 meaningfully. A Narval replication (job 59564082) showed consistent NDS/mAP (0.530/0.418) before the job crashed (training exitcode 1 after ~5h); no eval was run so planning metrics are unavailable for this run.

**Planning DN (DGX 3607 + Narval 59605629) — complete.** dnplan is worse than the baseline across all metrics. DGX: NDS 0.519 vs 0.523 (−0.004), mAP 0.404 vs 0.413 (−0.009), car_epa 0.488 vs 0.492 (−0.004), L2 0.674 vs 0.636 (+0.038), obj_box_col 0.123% vs 0.133% (≈flat). Narval replication (59605629) is consistent: NDS 0.526, mAP 0.413, L2 0.670, obj_box_col 0.111%. Both runs agree that planning DN degrades detection and planning relative to the baseline. The noisy ego trajectory queries likely interfere with the normal plan head during stage-2 training rather than regularising it. Planning DN should not be pursued further.

**Map DN (dndetmap, stage1) — bug found, fix applied.** Training on Apollo crashed repeatedly due to OOM in the eval hook's `collect_results_cpu` gather step; fixed by disabling the eval hook and resuming from iter_8780. Final checkpoint (iter_43900) eval: NDS 0.543, mAP 0.425 (detection healthy), but map mAP collapsed to 0.014 vs 0.559 for the det-DN-only baseline — a catastrophic regression not related to batch size (per-GPU BS=6 is within the safe range established in the April 1 investigation).

Root cause: `get_dn_anchors` in `map/target.py` stored `dn_pts_target` in [0,1] normalized space (output of `self.normalize_line()`), but `SparseLineLoss.forward()` calls `normalize_line()` on both prediction and target before computing the loss — identical to the normal loss path. This double-normalizes DN targets into a small range near [0.5, 0.53], misaligned with the [0,1] targets the normal map queries see. The DN loss converges to near-zero because both DN prediction and DN target are in the same wrong space, masking the bug, but the conflicting gradient signals on shared decoder parameters prevent the normal map queries from learning. `map_loss_line_5` plateauing at ~0.71 throughout training (matching the BS>6 failure pattern) is the signature.

Fix: `dn_pts_target` now uses raw BEV coordinates (before normalization), matching the format returned by `sample()` for the normal loss path. `dn_anchor` is unchanged (still [0,1] normalized, correct for the anchor deformable reference points).

Re-run with fix (Narval jobs 59688618 smoke, 59688619 extended, `stage1_4gpu_bs32_dndetmap`, 8/GPU): DN losses are now non-trivial (`map_loss_line_dn_5 ≈ 0.12` vs 0.0015 in the buggy run), confirming the coordinate fix generates real gradients. However, `map_loss_line_5` still plateaus near ~0.72 in the 4-GPU 8/GPU setting, so the original map-learning failure remains.

The control runs resolve the ambiguity:
- `stage1_4gpu_bs32` (no DN, 8/GPU, Narval job 59696538) also plateaus, so the failure is not caused by DN.
- `stage1_4gpu_bs24` (no DN, 6/GPU, Narval job 59696692) converges as expected, confirming that dropping back to 6/GPU restores normal map training in this 4-GPU stage-1 setup.

The conclusion is narrower than "8/GPU always fails" for stage 1, because the standard 8-GPU baseline at 8/GPU still trains normally. What the controls do show is that the problematic behavior in this report is tied to the 4-GPU 8/GPU recipe rather than to the DN formulation itself. Practically, map-DN follow-ups should stay on the known-safe 6/GPU setting unless the 4-GPU 8/GPU instability is explained separately.

## Future Work
